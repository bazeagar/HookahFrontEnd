const Tips = () => {
  const title = "Hook-aah Tipies";
  const heading = "Categories";
  return (
    <>
      <div>{title}</div>
      <p>
        <div align="left">{heading}</div>
      </p>
    </>
  )
};

export default Tips;
